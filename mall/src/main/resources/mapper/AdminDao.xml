<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mall.dao.AdminDao">
	<select id="loginAdmin" resultType="com.example.mall.entity.Admin">
		SELECT admin_id,name,password,enable_status,create_time,last_edit_time
		FROM tb_admin
		WHERE name = #{name} and password = #{password}
	</select>
</mapper>